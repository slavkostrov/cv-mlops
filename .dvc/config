[core]
    remote = s3_remote
['remote "myremote"']
    # needs oauth
    url = gdrive://1PRUo6x5rls8ZZNRk0634l-Rv6Qfq5A_f
['remote "s3_remote"']
    # public read-only
    url = s3://mlops-cats-dogs/
    endpointurl = https://storage.yandexcloud.net/
    region = ru-central1
    # doesn't need it for read-only
    access_key_id = KEY_ID
    secret_access_key = ACCESS_KEY
